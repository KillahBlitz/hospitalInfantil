function Platform({ user, catalogs }) {
    return (
        <div className="template-platform">
            <h1 className="content-title">Plataforma</h1>
            <p className="content-placeholder">Modulo de plataforma.</p>
        </div>
    );
}

export default Platform;
