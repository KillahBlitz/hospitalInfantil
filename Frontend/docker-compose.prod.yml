services:
  frontend:
    container_name: hospital-frontend-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      # Frontend expuesto en el puerto 5000 del host.
      - "5000:80"
    env_file:
      - .env
    restart: unless-stopped
